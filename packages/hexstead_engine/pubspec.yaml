name: hexstead_engine
description: Pure Dart rules engine for Hexstead. No Flutter dependencies.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  collection: ^1.19.0
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
